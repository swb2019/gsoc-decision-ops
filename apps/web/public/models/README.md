# Hourglass media assets (VO + 3D)

- `public/audio/voice/*.ogg` — ElevenLabs multi-voice (Daniel ops / Alice guidance / River dispatch)
- `public/models/channel_*.glb` — animated channel tokens
- `public/models/cop_markers.glb` — heat pulses + CCTV
- `public/models/console_props.glb` — radio + badge reader
